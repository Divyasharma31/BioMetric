version: '3.8'

services:
  api:
    build: .
    container_name: gridsphere_api
    restart: always
    ports:
      - "8001:8000"
    environment:
      # 1. Changed psycopg to asyncpg
      # 2. Changed sslmode=require to ssl=require
      # 3. Removed channel_binding (asyncpg doesn't use it the same way)
      - DATABASE_URL="postgresql+asyncpg://neondb_owner:YOUR_NEW_PASSWORD@ep-autumn-dew-an4wsq8n-pooler.c-6.us-east-1.aws.neon.tech/neondb?ssl=require"     
      
      # --- MQTT BROKER ---
      - MQTT_BROKER=mqtt.gridsphere.in 
      - MQTT_PORT=1883
      - MQTT_USER=esp8266
      - MQTT_PASS=strongpassword123